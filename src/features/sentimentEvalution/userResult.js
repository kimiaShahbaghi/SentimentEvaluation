//FIXME userResult reducer

// import { USER_RESULT } from "./actions";

// const userResultReducer = (state = { result: [] }, action) => {
//   switch (action.type) {
//     case USER_RESULT:
//       return { ...state, result: [...state.result, action.payload] };
//     default:
//       return state;
//   }
// };

// export default userResultReducer;
